# GitHub Pages base path — see vite.config.ts and docs/atlas-master-spec.md §4.
# Leave unset locally (defaults to /project-atlas/ for dev parity with CI).
# Set the real value once the repo path is confirmed.
VITE_BASE_PATH=/project-atlas/
