# Base path for the web build. See apps/web/vite.config.ts.
#
# - Leave unset for local dev, Vercel, and Docker (defaults to `/`).
# - Set to a subpath (e.g. `/mizan/`) only if you serve the build from a
#   subdirectory on a plain static host.
VITE_BASE_PATH=/
